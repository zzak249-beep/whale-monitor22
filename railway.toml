[build]
builder = "DOCKERFILE"

[deploy]
# Bot de background — sin servidor HTTP, sin healthcheck
healthcheckPath       = ""
healthcheckTimeout    = 0
restartPolicyType     = "ON_FAILURE"
restartPolicyMaxRetries = 10
