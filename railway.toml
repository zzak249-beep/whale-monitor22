[build]
builder = "DOCKERFILE"

[deploy]
# Worker de background — sin servidor HTTP, Railway NO debe hacer healthcheck
healthcheckPath         = ""
healthcheckTimeout      = 0
restartPolicyType       = "ON_FAILURE"
restartPolicyMaxRetries = 10
